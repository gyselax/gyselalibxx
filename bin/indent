#!/bin/bash

cd "$(dirname "$0")"
cd ..

docker run -v "$(pwd):/data" -u "$(id -u):$(id -g)" -ti jbigot/voicexx_env find src tests vendor/ddc vendor/sll '(' -name '*.h' -o -name '*.cpp' ')' -exec clang-format -i '{}' '+'
