concretizer:
  reuse: false
  targets:
    # Allow cross compiling.
    host_compatible: false
    # Target arcghitectures that are not specific to the computer, say,  avx_v3 vs znver3.
    granularity: generic
  unify: false
  duplicates:
    strategy: minimal
